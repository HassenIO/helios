<p class="hero"><%= t ".title_get_invite" %></p>

<%= form_for InvitationRequest.new, class: "login-form" do |f| %>
	<%= f.email_field :email, placeholder: "Votre adresse email" %>
	<%= f.submit "Demander mon code invitation", class: "btn on-hover btn-large btn-block" %>
<% end %>