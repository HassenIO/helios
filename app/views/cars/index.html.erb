<h1>Listing cars</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @cars.each do |car| %>
  <tr>
    <td><%= car.brand + '-' + car.model   %></td>
    <td><%= link_to 'Show', [@user,car] %></td>
    <td><%= link_to 'Edit', edit_user_car_path(@user,car) %></td>
    <td><%= link_to 'Destroy', [@user,car], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Car', new_user_car_path(@user) %>
