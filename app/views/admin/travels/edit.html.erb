<div class="row-fluid">
  <div class="span8">
    <h2><%= @travel.car.year.to_s + ' - ' + @travel.car.brand + " " + @travel.car.model %></h2>
  </div>

  <div class="span4">
    <%= link_to t(:back), admin_travels_path %>
  </div>

</div>


<%= render 'message' %>

<%= render 'form' %>



